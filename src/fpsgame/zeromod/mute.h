
// check for mute and spam
// returns: true if message should be muted, false if normal processing should take place
bool checkmute(clientinfo *ci, int type, const char *text)
{
    //TODO: do checking
    return false;
}
